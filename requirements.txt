# Core dependencies
chromadb>=0.4.24
sentence-transformers>=2.7.0
networkx>=3.3
tqdm>=4.66.0

# sentence-transformers pulls these in, but pin minimum versions for clarity
torch>=2.0.0
transformers>=4.40.0
huggingface-hub>=0.23.0

# Standard-library helpers (already in stdlib, listed for documentation only)
# pathlib  – stdlib
# json     – stdlib
# hashlib  – stdlib
